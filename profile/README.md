**Reactive Network** is an EVM automation layer built around Reactive Contracts (RCs) — event-driven smart contracts for cross-chain, on-chain automation. RCs monitor event logs across EVM chains and execute Solidity logic automatically when predefined conditions are met. Instead of waiting for users or bots to trigger transactions, RCs decide when to send cross-chain callback transactions, allowing smart contracts to coordinate conditional workflows across multiple chains.
